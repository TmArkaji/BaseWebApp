@page
@model LoginModel

@{
    Layout = "/Views/Shared/_EmptyLayout.cshtml";
    ViewData["Title"] = "Log in";
}

<div class="row vh-100 g-0">
    <div class="col-lg-6 position-relative d-none d-lg-block">
        <div class="bg-holder"></div>
    </div>

    <div class="col-lg-6">
        <div class="row align-items-center justify-content-center h-100 g-0 px-4 px-sm-0">

            <div class="col col-sm-6 col-lg-7 col-xl-6">

                <a href="#" class="d-flex justify-content-center mb-4">
                    <img src="~/images/logo.png" alt="" width="60" />
                </a>

                <div class="text-center mb-5">
                    <h3 class="fw-bold">@BaseWebApplication.Resources.Resource.General_LogIn</h3>
                    <p class="text-secondary">@BaseWebApplication.Resources.Resource.General_GetAccess</p>
                </div>

                <form id="account" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                    <div class="input-group mb-3">
                        <span class="input-group-text">
                            <i class="bx bx-user"></i>
                        </span>
                        <input asp-for="Input.Email" autocomplete="username" aria-required="true" class="form-control form-control-lg fs-6" placeholder="@BaseWebApplication.Resources.Resource.AppUser_UserName" />
                    </div>
                    <div>
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>
                    <div class="input-group" mb-3>
                        <span class="input-group-text">
                            <i class="bx bx-lock-alt"></i>
                        </span>
                        <input asp-for="Input.Password" autocomplete="current-password" aria-required="true" class="form-control form-control-lg fs-6" placeholder="@BaseWebApplication.Resources.Resource.AppUser_Password" />
                    </div>
                    <div>
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>

                    <div class="input-group mb-3 d-flex justify-content-between">
                        <div class="form-check">
                            <input asp-for="Input.RememberMe" class="form-check-input" />
                            <label asp-for="Input.RememberMe" class="form-label">
                                <input class="form-check-input" asp-for="Input.RememberMe" />
                                @BaseWebApplication.Resources.Resource.General_RememberMe
                            </label>
                        </div>
                        <div>
                            <small><a id="forgot-password" asp-page="./ForgotPassword">@BaseWebApplication.Resources.Resource.General_ForgotPassword</a></small>
                        </div>
                    </div>

                    <button class="btn btn-primary btn-lg w-100 mb-3">@BaseWebApplication.Resources.Resource.General_LogIn</button>
                </form>

                <div class="text-center">
                    <small>Don't have an account? <a href="#" class="fw-bold">Sign up</a></small>
                </div>
            </div>

        </div>

    </div>

</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
